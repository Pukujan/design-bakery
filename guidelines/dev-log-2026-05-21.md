# Dev log — 2026-05-21

| Field | Value |
|-------|-------|
| **Document date** | 2026-05-21 |
| **Created** | 2026-05-21 |
| **Last updated** | 2026-05-21 |

**Branch:** `main` (at `267e9d8` for Mermaid zoom; local WIP for blog-detail move)  
**Build:** `pnpm run build` — passed  
**Dev URL:** http://localhost:5300

---

## Human summary

Two threads of work:

1. **Shipped on GitHub (`main`)** — Interactive **Mermaid zoom** on engineering blog detail: toolbar (+/−, slider), scrollable frame for large charts, pinch/Ctrl/⌘/Shift+wheel zoom toward the cursor, and page scroll when the inner frame hits an edge.

2. **Local (not yet committed)** — Moved canonical **blog detail** from `extras/design changes to blog details/` into `src/app/modules/engineering/BlogDetailPage/`, archived the Figma export, removed `@blog-detail-v2` alias.

---

## Mermaid zoom (on `main`)

| Feature | Behavior |
|---------|----------|
| **Toolbar** | Zoom out, zoom in, % label, slider (75%–300%) |
| **Scroll frame** | Large charts or zoom &gt; 100% → `max-height: min(70vh, 520px)`, scroll inside |
| **Pinch** | Two fingers on chart (phone + trackpad) |
| **Ctrl/⌘ + scroll** | Trackpad pinch zoom on laptop |
| **Shift + scroll** | Mouse wheel zoom over the chart |
| **Focal zoom** | Zoom stays under cursor / pinch center (not top-left) |
| **Scroll chaining** | `overscroll-behavior: auto` — at edge, page scroll continues |

**Canonical code (after local move):** `src/app/modules/engineering/BlogDetailPage/MermaidDiagram.tsx`  
**On `main` before move:** `extras/design changes to blog details/app/components/MermaidDiagram.tsx`

**Test:** http://localhost:5300/endtoend-engineer/blogs/7

### Commits on `main` (Mermaid)

| Commit | Summary |
|--------|---------|
| `267e9d8` | Shift+scroll wheel zoom |
| `9d65b7b` | Focal zoom (cursor / pinch center) |
| `1e149a2` | Wheel scroll chains to page at edge |
| `494427e` | `overscroll-behavior: auto` (fix scroll trap) |
| `32505b1` | Pinch + trackpad on laptops |
| `1424173` | Pinch on touch devices |
| `9111046` | Scroll viewport (replaced magnifier) |
| `9ba703b` | Zoom toolbar + slider |

---

## Blog detail → `src/` (local WIP)

| Area | What changed |
|------|----------------|
| **Canonical paths** | `BlogDetailPage.tsx`, `MermaidDiagram.tsx` under `src/app/modules/engineering/BlogDetailPage/` |
| **Archive** | `extras/design changes to blog details/` → `archive/figma-export-blog-detail-2025-05/` |
| **Config** | Removed `@blog-detail-v2` from `vite.config.ts`, `tsconfig.json`; Tailwind `@source` cleanup |
| **Types** | `isValidElement<{ children?: ReactNode }>` for TOC heading slugs |

---

## For agents

| Topic | Doc |
|-------|-----|
| **Index** | `guidelines/agent-devlog-index.md` |
| **Mermaid** | `guidelines/agent-devlog-mermaid.md` — zoom, scroll, focal point, modifiers |
| **Blog posts** | `guidelines/agent-devlog-engineering-blog-posts.md` |
| **Blog motion** | `guidelines/agent-devlog-blog-motion.md` |
| **CodeGraph** | `guidelines/agent-devlog-codegraph.md` |

**Do not** re-run `mermaid.render` when only `zoom` state changes (caused reset-to-100% bug; fixed by stable `measureChart` deps).

---

## CodeGraph

```bash
pnpm run codegraph:sync    # incremental (2026-05-21 session)
pnpm run codegraph:status
# Full reindex if needed: codegraph index -f
```

**Sync result (2026-05-21):** 4 changed files, 49 nodes added in ~757ms (reflects `MermaidDiagram` path under `src/`).

**Config:** `.codegraph/config.json` excludes `archive/**` and legacy `extras/design changes to blog details/**`.

---

## Local URLs

| Page | URL |
|------|-----|
| Blog list | http://localhost:5300/endtoend-engineer/blogs |
| Mermaid stress test | http://localhost:5300/endtoend-engineer/blogs/7 |
| Blog detail | http://localhost:5300/endtoend-engineer/blogs/1 |

---

## Next steps

- Commit + push blog-detail move to `src/` when ready.
- Optional: same archive pattern for other `extras/` case-study trees already partially in `src/`.
