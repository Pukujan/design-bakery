# Dev log — 2026-05-20

**Branch:** `feature/multi-portfolio`  
**Build:** `pnpm run build` — passed  
**Dev URL:** http://localhost:5300

---

## Human summary

We shipped the **new blog experience** (list + article pages from the extras design), made blogs sort **newest first**, fixed **Mermaid diagrams** so they stay reliable, added **home-style floating animations** on blog pages, and set up **agent docs + CodeGraph** so future AI edits are safer.

---

## What shipped

| Area | What changed |
|------|----------------|
| **Blog detail** | Version B only (`extras/.../BlogDetailPage.tsx` via `@blog-detail-v2`). Sticky sidebar (≥1020px), TOC anchor links, loading skeleton, dark fenced code blocks. |
| **Blog list** | Same visual language; dynamic floats + bouncy cards via `BlogPageMotion`. |
| **Mermaid** | `mermaid.render()` per chart + CSS on `.blog-mermaid-chart` (no `theme: base` / no post-render SVG hacks). |
| **Data** | `sortBlogsByDateDesc()` + Firestore `orderBy('numericId', 'desc')`. |
| **Nav** | Blog detail: hamburger below 1020px; categories in mobile menu. |
| **Dev** | Vite port **5300**. |
| **Agents** | Topic devlogs (Mermaid, blog motion, CodeGraph), contract, `AGENTS.md`, Cursor rules. |
| **CodeGraph** | `.codegraph/config.json`, npm scripts; DB is local only (not committed). |

---

## For agents (read first)

| Topic | Doc |
|-------|-----|
| **Index** | `guidelines/agent-devlog-index.md` |
| **Workflow** | `guidelines/agent-devlog-contract.md` |
| **Mermaid** | `guidelines/agent-devlog-mermaid.md` |
| **Blog motion** | `guidelines/agent-devlog-blog-motion.md` |
| **CodeGraph** | `guidelines/agent-devlog-codegraph.md` |

**Test after blog/Mermaid edits:** `/endtoend-engineer/blogs` and `/endtoend-engineer/blogs/1`

---

## Key files

- `extras/design changes to blog details/app/components/BlogDetailPage.tsx`
- `src/app/components/BlogPageMotion.tsx`
- `src/app/modules/engineering/BlogListPage/BlogListPage.tsx`
- `src/app/modules/engineering/blogData.ts`
- `src/styles/globals.css`
- `.cursor/rules/*.mdc`

---

## Setup after clone

```bash
pnpm install
pnpm run dev          # port 5300
npx @colbymchenry/codegraph   # once, global MCP
pnpm run codegraph:init       # per machine, builds .codegraph/codegraph.db
```

---

## Next (optional)

- Move extras blog detail into `src` when design is final.
- Wire `EngineeringInsights` to `BlogPageDecor` for one shared decor module.
