# Cloud Functions env lives in the repo root — use ../.env (see ../.env.example).
# This file is kept only as a pointer; you do not need functions/.env anymore.
#
# If you still have secrets here, move them to ../.env and delete functions/.env.
