# Moved

Blog detail production code now lives in:

- `src/app/modules/engineering/BlogDetailPage/`

Legacy Figma export (reference only):

- `archive/figma-export-blog-detail-2025-05/`

**Close any editor tabs** still showing the old ~475-line `BlogDetailPage.tsx` — choose *Revert* or close *without saving*. The only file here now is a 1-line re-export shim pointing at `src/`.

**Edit here:** `src/app/modules/engineering/BlogDetailPage/BlogDetailPage.tsx`
