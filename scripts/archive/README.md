# Archived scripts

Not used by default dev or deploy. Kept for reference or one-off Firebase RTDB/Firestore migrations.

| Script | Notes |
|--------|--------|
| `sync-json-to-firebase.mjs` | Pre-monorepo JSON → Firebase sync (referenced old `src/app`) |
| `seed-portfolios-firestore.ts` | One-time Firestore CMS seed — `pnpm run seed:firestore` if still needed |
