# TASK-DB-0049 — repo cleanup

| Field | Value |
|-------|-------|
| **Created** | 2026-09-24 |
| **Branch** | `task/TASK-DB-0049-repo-cleanup` |
| **Status** | PR open against `main` — awaiting review (do not merge without owner approval) |

## Goal

Make the repo lighter and easier for agents to navigate: archive unused home profiles, prune stale logs/docs, fix broken agent doc paths, stop tracking build outputs, dedupe the ONI case-study assets, add a light continuity setup, and write a modularization plan. No history rewrite, mp4 untouched.

## Done

- Home profiles: removed `default`, `ai-engineer`, `legal-workflow-engineer`, `forward-deployed-engineer` JSON + `PortfolioHub.tsx`/`hubDestinations.ts` + stub re-exports `components/EngineeringHome.tsx`, `components/DesignPortfolio.tsx`. `PortfolioId` is now only `endtoend-engineer`; `portfolioDefaults.ts`, `ADMIN_PORTFOLIOS`, seed list, admin switcher and never-mounted design admin routes simplified. Rendered homepage content unchanged (the experience list it rendered moved to `endtoend-engineer/engineering/relevant-experience-rendered-list.json`).
- Pruned: May 2026 dev logs, Fluffy round-2/3 + v2/v3 logs, `additionals/doc/audit log/ux/`, `reviewed/`, `additionals/doc/useful-prompts/`, `scripts/archive/`, `additionals/archive/*` except `firebase/` (still read by scripts). Devlog index + docs updated.
- `AGENTS.md` paths fixed (`additionals/guidelines/…`, `additionals/doc/…`) and continuity links added.
- `.gitignore`: `*.tsbuildinfo`, `frontend/public/sitemap.xml` (regenerated by frontend `prebuild` on every build incl. Vercel); both tsbuildinfo files and the sitemap untracked.
- ONI v3/v4 case studies share `frontend/extras/oni_agent_shared_assets` via `@oni-agent-assets` alias (15 identical files, ≈ 2.06 MB removed).
- Owner decision (2026-09-24): live homepage (endtoend-engineer, Fitzgerald-first experience list) is correct — kept as-is. Deleted the unreachable design-portfolio admin editors (`AboutEditor`, `SkillsEditor`, `AdvocacyEditor`, `ArtGalleryEditor`, `WebShowcaseEditor`, `GalleryPageEditor`), `frontend/src/app/modules/design/`, `components/galleryData.ts`, and the design-only CMS getters/setters + JSON imports in `adminContentService.ts`.
- Added `HANDOFF.md`, `checkpoints/CURRENT.md`, this file, `additionals/doc/modularization-plan.md`.

## Evidence

- All `ci.yml` steps pass locally on Node 24.21.0 / pnpm 10.32.1 (lint, build, frontend typecheck, backend build, publish-kit fonts test, homepage content stability).
- Build output asset filenames/hashes for the ONI images are identical before/after the dedupe.
- Dev servers: `/`, `/admin`, `/admin/endtoend-engineer`, `/case-studies/ai-agents/v3|v4` return 200 and their module graphs transform without Vite errors.
- PR description lists removed paths and sizes.

## Next step

- Owner reviews/merges the PR.
- Fluffy V4 handoff/notes are kept as source material (used by TASK-DB-0050).
- Low-risk follow-ups: `endtoend-engineer/engineering/skill-categories.json` and `skills-meta.json` are not rendered (homepage uses `modules/engineering/EngineeringSkills/skill-categories.json` and `ENGINEERING_SKILLS_META_DEFAULT`; `skills-meta.json` is only asserted by `test:homepage-content`) — reconcile. Then pick up steps 1–4 of the modularization plan.
