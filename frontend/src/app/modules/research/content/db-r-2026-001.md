# Cortex reliability kernel: composition status and evidence boundaries

> **Owner gate.** This document is a scaffold for arXiv-style research presentation. Claims, tables, and citations below are structural placeholders until the owner marks the paper `approved` in `catalog.js` and accepts the numeric content.

## Abstract

Cortex is reliability infrastructure for AI agent runtimes: contract-first process, deny-by-default authorization, content-bound receipts, and independent evaluation. This note separates *component-tested* mechanisms from *composed product path* status, and states documentation rules for benchmarks so green tests are not silently promoted to production-governance claims.

## 1. Scope and non-claims

- Does not claim live Cortex-governed production runs unless an approved table says so.
- Does not treat builder self-report as independent evaluation.
- Composition status remains blocked until an owner-approved recovery/holdout path says otherwise.
- Does not invent metrics, pass rates, or latency figures for empty benchmark cells.

## 2. Method (documentation)

Public research entries require: stable id, authors, abstract, citation list with resolvable links or in-repo paths, and explicit status (`pending` | `approved`). Benchmark rows require method name, date, environment, and artifact pointer. Literature-facing claims should prefer verified identifiers (DOI, arXiv id) over ungrounded paraphrase; anti-hallucination filters for citations are out of band for this scaffold but required before any `approved` literature list.

## 3. Evidence boundaries

Component suites and sealed holdouts may exist in engineering workspaces; that does not authorize public product claims. Status language on this site tracks `catalog.js` only. Narrative product framing lives in the case study paths below and is not a substitute for owner-approved research tables.

The matrix below is a *qualitative* boundary map. It records what each mechanism demonstrates in isolation versus its status in the reviewed composition branch. It carries **no numeric claims**; every statement is sourced from the Cortex case study narrative, not from a benchmark run. Read it as scope, not score.

*Mechanism evidence vs. composition status (qualitative; no metrics).*

| Mechanism | Component evidence (isolated) | Composition status (reviewed branch) |
| --- | --- | --- |
| Contract-first plan | Merged and component-tested. | **Blocked**, composition path has not demonstrated a durable owner-frozen plan receipt. |
| Deny-by-default apply gate | Standalone pure path gate, tested in isolation. | **Blocked**, reviewed composition calls the gate directly without a broker-mediated apply boundary. |
| Content-bound receipts | Receipt components merged and tested. | **Blocked**, composition accepts an arbitrary callback result as its approval receipt; no exact-diff-bound authority receipt is verified. |
| Independent evaluation / recovery | Independent composition-recovery review performed; eight focused tests observed passing. | **Blocked**, the projection permitted `APPROVED` without a hash and recovery joined receipt data back into the run, violating the recorded v1.4 recovery decision. Repair path: builder repair → independent holdout → owner review. |

## 4. Benchmarks (owner approval required)

**No benchmark table in this revision.** This paper makes no numeric claim, so an empty table would imply results that do not exist. Measured results live in the repository ledger `evals/results.jsonl`, where every figure carries a provenance tag. Figures are cited from there, never restated here.

**No benchmark table in this revision.** This paper makes no numeric character-stack claim, so an empty table would imply results that do not exist. Measured results for the kernel and evaluation lanes live in the repository ledger `evals/results.jsonl`, where every figure carries a provenance tag (`committed-artifact` / `recomputed` / `reconciled` / `prose-only`). Figures are cited from there, never restated here.

## 5. Related case study (layout paths)

Narrative product framing (not a research paper). Design review variants:

- [Index](/case-studies/cortex/)
- [Path A](/case-studies/cortex/a/)
- [Path B](/case-studies/cortex/b/)
- [Path C](/case-studies/cortex/c/)

## References

1. Design Bakery. *Cortex case study*. 2026. [/case-studies/cortex/](/case-studies/cortex/) · [A](/case-studies/cortex/a/) · [B](/case-studies/cortex/b/) · [C](/case-studies/cortex/c/)
2. Stupidly Simple Cortex. *Start here / capability status* (in-repo harness docs). Local workspace documentation; not an external peer-reviewed source.

---

Cite as: Pujan (2026). *Cortex reliability kernel: composition status and evidence boundaries*. Design Bakery Research db-r-2026-001 (pending).

