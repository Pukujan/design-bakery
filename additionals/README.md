# Additionals

Support material moved out of the repo root to keep the workspace cleaner.

## Contents

- `archive/firebase/` - legacy Firebase config still read by migration/CORS scripts (other archive material was pruned in TASK-DB-0049; see git history)
- `deploy/` - deploy env exports and related notes
- `doc/` - architecture, routes, env, and project docs
- `guidelines/` - long-lived dev logs and topic notes

Keep active app code in `backend/`, `frontend/`, `packages/`, `scripts/`, and the core root config files.
