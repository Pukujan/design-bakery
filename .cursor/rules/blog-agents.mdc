---
description: Blog agents module — contracts, flags, phased rollout
globs: src/app/modules/agent/**,src/app/modules/admin/adminRoutes.tsx,src/app/modules/admin/AdminLayout.tsx,guidelines/agent-devlog-blog-agents.md
alwaysApply: false
---

# Blog agents module

Read **`guidelines/agent-devlog-blog-agents.md`** before changes.

- **SEO rules:** always in admin `BlogSeoPanel`; writes via `saveBlog()` only
- **Public meta:** `BlogPostHead` in engineering module — not agent imports
- **Flag:** `VITE_ENABLE_BLOG_AGENTS` — default off until Slice 3 (Functions)
- **Do not** import agent module into blog detail/list/Mermaid
- **Do not** put OpenRouter keys in client env
- **Writes:** `saveBlog()` only; `agent_*` Firestore collections for sessions/audit
