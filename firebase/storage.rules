rules_version = '2';
// Emulator-only permissive rules for local blog image uploads.
// Production deploy should use stricter rules before going live.
service firebase.storage {
  match /b/{bucket}/o {
    match /blog-publish/{postId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
