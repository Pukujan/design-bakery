# Archive

Historical exports and removed code kept for reference. **Not indexed by CodeGraph** (see `.codegraph/config.json` exclude).

| Path | Contents |
|------|----------|
| `figma-export-blog-detail-2025-05/` | Figma Make export snapshot (May 2025) |
| `frontend-legacy/` | Unused `BlogPage.tsx`, `BlogPostPage.tsx` superseded by `modules/blog/public/*` |

Active case-study sandboxes moved to **`frontend/extras/`** (wired in Vite/tsconfig).

Do not import from here in active app code.
