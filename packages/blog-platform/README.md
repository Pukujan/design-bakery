# Blog platform packages

Modular blog creator export on branch `feature/blog-platform-packages`.

See [EXPORT.md](./EXPORT.md) for copying into a new repo.

```bash
pnpm run dev:blog-studio   # reference app (port 5310)
pnpm run dev:stack         # API for login + publish kit
```
