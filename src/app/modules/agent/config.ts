/** Slice 3 — on when Functions + OpenRouter secret are deployed. */
export const BLOG_AGENTS_ENABLED = import.meta.env.VITE_ENABLE_BLOG_AGENTS === 'true';

export const AGENT_API_VERSION = 1 as const;
