{
  "name": "design-bakery-api",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --dir services run build && tsc",
    "start": "node lib/server.js",
    "dev": "pnpm --dir services run build && tsx watch src/server.ts",
    "env:split": "node ../scripts/split-env.mjs"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
